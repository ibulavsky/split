import React from "react";

// Events page allows to create and edit transactions (events).
export const Events = () => {
    return <div>
        Events page allows to create and edit transactions (events).
    </div>
}